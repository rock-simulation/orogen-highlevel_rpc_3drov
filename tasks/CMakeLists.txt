# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(highlevel_rpc_3drovTaskLib)
ADD_LIBRARY(${HIGHLEVEL_RPC_3DROV_TASKLIB_NAME} SHARED 
    ${HIGHLEVEL_RPC_3DROV_TASKLIB_SOURCES})
add_dependencies(${HIGHLEVEL_RPC_3DROV_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${HIGHLEVEL_RPC_3DROV_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${HIGHLEVEL_RPC_3DROV_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${HIGHLEVEL_RPC_3DROV_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${HIGHLEVEL_RPC_3DROV_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${HIGHLEVEL_RPC_3DROV_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${HIGHLEVEL_RPC_3DROV_TASKLIB_HEADERS}
    DESTINATION include/orocos/highlevel_rpc_3drov)

